/*var assert = require('assert');

var Chance = require('chance');

var chance = new Chance();


describe('splitscnd Welcome', function() {
    it('should Welcome Message', function () {
    	this.timeout(60000);//set timeout to be a minute
        
        browser.waitForVisible('#text');

	    var confirmText = browser.getText('#text');

	    assert.equal(confirmText,"Now, let’s take 5 minutes to personalize your account to make sure that you’re receiving the most accurate and up-to-date information, Ready, set, go!");
        
        browser.click('#get-started');

        browser.pause(3000);


    	};
    };
*/